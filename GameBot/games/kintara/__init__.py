"""Kintara game plugin package.

The plugin is discovered from games.kintara.plugin by the platform loader.
Avoid importing it eagerly here so lightweight modules such as the shared
Ember formatter and monitor can run independently.
"""
