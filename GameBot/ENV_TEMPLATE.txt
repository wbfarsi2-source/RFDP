# ------------------------------------------------------------
# GameBot Platform v0.7.0
# Copy this file to .env and replace every required placeholder.
# Never commit .env or share it publicly.
# ------------------------------------------------------------

# Network routing
# auto: detect Windows/PAC/WinHTTP/local proxy, otherwise use direct connection
# Manual examples: http://127.0.0.1:7890 or socks5://127.0.0.1:10808
GAMEBOT_PROXY_URL=auto

# Telegram
TELEGRAM_BOT_TOKEN=REPLACE_WITH_NEW_BOT_TOKEN
ADMIN_USER_IDS=REPLACE_WITH_ADMIN_TELEGRAM_ID

# Storage and encryption
DATABASE_URL=sqlite+aiosqlite:///./data/gamebot.db
MASTER_KEY=REPLACE_WITH_A_NEW_RANDOM_MASTER_KEY
LOG_LEVEL=INFO

# Support
SUPPORT_HANDLE=@YourSupport
SUPPORT_URL=https://t.me/YourSupport
DISPLAY_TIMEZONE=Asia/Tehran

# Runtime supervision
WORKER_HEARTBEAT_TIMEOUT=90
WORKER_RESTART_LIMIT=5
EXPIRY_CHECK_SECONDS=60
EXPIRY_WARNING_HOURS=6

# Backup
BACKUP_INTERVAL_SECONDS=86400
BACKUP_KEEP_LAST=14
BACKUP_INCLUDE_ENV=false

# Payment verification
PAYMENT_CHECK_SECONDS=30
PAYMENT_MIN_CONFIRMATIONS=1
PAYMENT_AMOUNT_TOLERANCE_USDC=0.000001
PAYMENT_UNIQUE_AMOUNT_ENABLED=false
PAYMENT_UNIQUE_AMOUNT_MAX_UNITS=999
PAYMENT_MAX_PREORDER_AGE_SECONDS=300
USDC_DECIMALS=6

# Solana USDC
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
SOLANA_USDC_WALLET=REPLACE_WITH_SOLANA_WALLET
SOLANA_USDC_MINT=REPLACE_WITH_OFFICIAL_USDC_MINT

# Base USDC
BASE_RPC_URL=https://mainnet.base.org
BASE_USDC_WALLET=REPLACE_WITH_BASE_WALLET
BASE_USDC_CONTRACT=REPLACE_WITH_OFFICIAL_USDC_CONTRACT

# Optional remote feature flags
FEATURE_FLAGS_URL=
FEATURE_FLAGS_TOKEN=
FEATURE_FLAGS_REFRESH_SECONDS=60

# Kintara
KINTARA_BASE_URL=https://kintara.gg

# Shared project cookie used only by Kintara shared services such as Molten Location.
# Paid account runtimes never use this cookie.
KINTARA_COOKIE=__Host-kintara_session=REPLACE_WITH_PROJECT_COOKIE

# Optional dedicated override for Molten Location. Leave empty to use KINTARA_COOKIE.
KINTARA_EMBER_COOKIE=

# Default Kintara plan prices and durations.
# Every plan can later be changed or switched between free/paid from /admin.
KINTARA_FISHING_PRICE_USDC=1.00
KINTARA_FISHING_COOK_PRICE_USDC=1.99
KINTARA_FISHING_COOK_SPINNER_PRICE_USDC=2.99
KINTARA_MOLTEN_PRICE_USDC=1.00
KINTARA_PLAN_DURATION_DAYS=7
KINTARA_MOLTEN_DURATION_DAYS=30

# Kintara trial
KINTARA_TRIAL_ENABLED=true
KINTARA_TRIAL_DURATION_MINUTES=120
KINTARA_TRIAL_SLOT_LIMIT=0

# Private Kintara information channel
# The bot must be an administrator with invite, ban/unban, post and edit permissions.
KINTARA_CHANNEL_ID=-1004463401405
KINTARA_CHANNEL_TITLE=Info Kintara
KINTARA_CHANNEL_POST_INTERVAL_SECONDS=60
KINTARA_CHANNEL_INVITE_EXPIRE_SECONDS=900

# Optional: put an old permanent invite here once. The bot revokes it after startup.
KINTARA_CHANNEL_LEGACY_INVITE_LINK=
